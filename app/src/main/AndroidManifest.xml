<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alibaba.android.arouter.app">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NLRouter">

        <activity
            android:name=".LaunchDispatchActivity"
            android:theme="@android:style/Theme.NoDisplay">
            <!--Default-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Scheme -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="arouter" />
            </intent-filter>
            <!-- App Links -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="arouter.app.com"
                    android:scheme="http" />
                <data
                    android:host="arouter.app.com"
                    android:scheme="https" />
            </intent-filter>
        </activity>
        <!--<activity-->
        <!--    android:name=".SchemeFilterActivity"-->
        <!--    android:theme="@android:style/Theme.NoDisplay">-->
        <!--    &lt;!&ndash; Scheme &ndash;&gt;-->
        <!--    <intent-filter>-->
        <!--        <action android:name="android.intent.action.VIEW" />-->

        <!--        <category android:name="android.intent.category.DEFAULT" />-->
        <!--        <category android:name="android.intent.category.BROWSABLE" />-->

        <!--        <data-->
        <!--            android:scheme="arouter" />-->
        <!--    </intent-filter>-->
        <!--    &lt;!&ndash; App Links &ndash;&gt;-->
        <!--    <intent-filter>-->
        <!--        <action android:name="android.intent.action.VIEW" />-->

        <!--        <category android:name="android.intent.category.DEFAULT" />-->
        <!--        <category android:name="android.intent.category.BROWSABLE" />-->

        <!--        <data-->
        <!--            android:host="arouter.app.com"-->
        <!--            android:scheme="http" />-->
        <!--        <data-->
        <!--            android:host="arouter.app.com"-->
        <!--            android:scheme="https" />-->
        <!--    </intent-filter>-->
        <!--</activity>-->
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".AccountActivity" />

        <meta-data
            android:name="arouter_scheme"
            android:value="arouter" />

        <meta-data
            android:name="arouter_host"
            android:value="arouter.app.com" />

    </application>

</manifest>